
包装类：基本数据类型对应的复杂类型
作用：提升基本数据类型的能力，具备面向对象的能力。


Byte，Short，Integer，Long，Float，Double
Boolean
Charecter

装箱，拆箱（数据类型转换）
自动装箱，自动拆箱（自动数据类型转换）

集合：类 
作用：放多个。
单列：Collection接口---List接口
--ArrayList  是一个数组，取的快，插入慢
--LinkedList 是一个链表，取的慢，插入快,实现Stack的功能

Set
--HashSet 无顺序，不可重复，可增，可删，不可改，通过迭代器遍历
--TreeSet 无序，不可，可增，可删，不能set，get，可遍历

研究方式：
new对象
增：
删：
改：
查：
顺序：有序（放进来的顺序），
重复：可重复


总结：处理多个数据（同类型）。

泛型：未来的类型（暂时（定义的时候）不明确，未来（使用的时候）是明确的）





















